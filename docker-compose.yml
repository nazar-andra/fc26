services:
  fc26:
    build: .
    container_name: fc26
    restart: unless-stopped
    ports:
      - "3004:3004"
    environment:
      PORT: "3004"
      HOST: "0.0.0.0"
      DATA_DIR: /app/data
      ADMIN_USER: admin
      ADMIN_PASS: "123"
    volumes:
      - ./data:/app/data
